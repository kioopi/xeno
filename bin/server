#!/bin/bash
# Start Phoenix server with a named node for remote console access
#
# Usage:
#   ./server
#
# This allows you to connect from another terminal using: ./console

iex --sname xeno@localhost -S mix phx.server
